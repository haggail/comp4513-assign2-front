/*import React, { Component } from 'react';
import { Link } from 'react-router-dom';


class SingleUserPortfolioItem extends Component {
    render() {
        return (
            <tr>
                <td>
                    <Link className="has-text-dark" to={"/stocks/" + this.props.identifier} >
                    {this.props.symbol}
                    </Link>
                </td>
                <td>
                    <Link className="has-text-dark" to={"/stocks/" + this.props.identifier} >
                    {this.props.children}
                    </Link>
                </td>
                <td>{this.props.amount}</td>
            </tr>
            );
    }
}

export default SingleUserPortfolioItem;*/