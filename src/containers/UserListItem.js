/*import React, { Component } from 'react';
import { Link } from 'react-router-dom';


class UserListItem extends Component {
    render() {
        return (
                <Link to={"/users/" + this.props.identifier} className="panel-block">
                    {this.props.children}
                </Link>
            );
    }
}

export default UserListItem;*/